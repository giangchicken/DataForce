"""DataForce: gated stages that turn a raw corpus into a documented dataset.

A run is one modality by one profile. Everything else -- the stages, the gates,
the invariants -- is written once and knows neither.
"""

__version__ = "0.1.0"

__all__ = ["__version__"]
