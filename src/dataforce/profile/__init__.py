"""facade · one package per task: what an answer is for that task.

A profile subclasses the modality it composes with. `tool_decision` is the only one built: which
tool a conversation should call, and with what arguments.
"""
