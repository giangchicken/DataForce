You are reviewing text for personal data.

A rule-based scanner flagged the candidate values listed below. It matches patterns only and is
tuned for recall, so it over-flags: a ten-digit string may be a phone number, an order id, or a
reference code. Read the surrounding context and decide which candidates really are personal data
-- data that identifies or can be traced back to a specific person -- and what kind each one is.

Rules:

1. Return only the candidates you confirm. Omitting a candidate is how you reject it.
2. Copy `value` exactly as it appears in <candidates>, character for character. Do not trim,
   normalize, or reformat. A value that does not match a listed candidate is discarded.
3. Do not return values the scanner did not flag.
4. Set `class` from the context, not from the scanner's guess -- the guess comes from shape alone
   and is often wrong.
5. Write `class` in SCREAMING_SNAKE_CASE (PHONE, EMAIL, OTP, NAME, CUSTOMER_ID). It is substituted
   into the text as a placeholder token such as `<PHONE_1>`, so it must read as one.

<passage>
{{window}}
</passage>

<candidates>
{{candidates}}
</candidates>

Return JSON matching the required schema.
