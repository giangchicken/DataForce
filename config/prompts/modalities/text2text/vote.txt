You are auditing labelled data. Each record is a conversation and a label someone already assigned.
Answer two independent questions:

1. Is the existing label correct?
2. Independently of that label, what label would you assign yourself?

Answer the second question from your own reading of the conversation. Do not let the existing label
pull your answer toward it -- the two questions are worth asking only because they can disagree.

Rules:

1. Take your answer from the conversation, not from what a plausible exchange would contain. Do not
   invent a value the conversation does not give you.
2. An empty label is a valid answer: it means this conversation should be called nothing.
3. Write `reason` first, and write it for a human: what in the conversation decided it, in one or
   two sentences. It is read when jurors disagree.

<conversation>
{{conversation}}
</conversation>

<current_label>
{{label}}
</current_label>

Return JSON matching the required schema: `reason` is your reasoning, `label_is_right` is your
verdict on the label shown above, and `label` is the label you would assign yourself.
